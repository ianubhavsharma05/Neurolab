# NOTE: torch, torchvision, torchaudio are installed separately in the Dockerfile
#       via --index-url https://download.pytorch.org/whl/cpu (CPU-only wheels)
# NOTE: grad-cam is installed with --no-deps in the Dockerfile to prevent
#       its opencv-python dep from conflicting with opencv-python-headless

# ── Numpy: MUST be 1.x — PyTorch 2.2.x is incompatible with numpy 2.x ────────
numpy==1.26.4

# ── Application dependencies ───────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
python-multipart==0.0.9
librosa==0.10.2
joblib==1.4.2
scikit-learn==1.2.2
opencv-python-headless==4.9.0.80
pillow==10.3.0
matplotlib==3.9.0
pydantic==2.7.1
python-dotenv==1.0.1

# Dependencies for grad-cam (since we install it with --no-deps)
ttach==0.0.3
tqdm
