--extra-index-url https://download.pytorch.org/whl/cpu

# ── Numpy: REQUIRED for unpickling the 2026 speech model ───────────────────────
# Note: Upgraded back to 2.1.x to match your pickled model's MT19937 BitGenerator architecture.
numpy==2.1.1

# ── Application dependencies ───────────────────────────────────────────────────
fastapi>=0.115.0
starlette>=0.40.0
uvicorn>=0.30.0
python-multipart>=0.0.9
librosa==0.10.2
joblib>=1.4.2
scikit-learn==1.8.0
opencv-python-headless==4.10.0.84
pillow==10.4.0
matplotlib==3.9.2
pydantic>=2.9.0
python-dotenv==1.0.1

# ── AI Core Dependencies (CPU Optimized for Render/Railway/Vercel) ─────────────
# IMPORTANT: These versions are strictly coordinated with the Dockerfile
torch==2.4.1+cpu
torchvision==0.19.1+cpu
torchaudio==2.4.1+cpu
grad-cam==1.5.0
ttach==0.0.3
tqdm
