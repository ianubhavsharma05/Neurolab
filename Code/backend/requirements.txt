# NOTE: torch, torchvision, torchaudio are installed separately in the Dockerfile
#       via --index-url https://download.pytorch.org/whl/cpu (CPU-only wheels)
# NOTE: grad-cam is installed with --no-deps in the Dockerfile to prevent
#       its opencv-python dep from conflicting with opencv-python-headless

# ── Numpy: 2.x is REQUIRED for unpickling the 2026 speech model ────────────────
numpy==2.4.3

# ── Application dependencies ───────────────────────────────────────────────────
fastapi==0.111.0
starlette==0.37.2
uvicorn==0.29.0
python-multipart==0.0.9
librosa==0.10.2
joblib==1.4.2
scikit-learn==1.2.2
opencv-python-headless==4.8.0.76
pillow==10.3.0
matplotlib==3.7.1
pydantic==2.7.1
python-dotenv==1.0.1

# ── AI Core Dependencies ───────────────────────────────────────────────────────
# NOTE: these are CPU-only versions for Render/Railway, set up correctly here
torch>=2.2.0
torchvision>=0.17.0
grad-cam==1.5.0
ttach==0.0.3
tqdm
